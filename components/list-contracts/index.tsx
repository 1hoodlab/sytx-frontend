import { Box } from "@chakra-ui/react";
import React from "react";

type Props = {};

export const ListContracts = (props: Props) => {
  return (
    <Box flex={1} h={"100%"} padding={"40px 20px 20px 20px"}>
      123
    </Box>
  );
};
